{
  "name": "example-svelte-host",
  "version": "0.0.0",
  "private": true,
  "description": "Minimal Svelte 5 host for <tessera-respond> — no wrapper needed: Svelte sets camelCase props as DOM properties and its event attributes handle the colon in tessera:* names. Built in CI so framework compatibility regressions fail a build.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "type-check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "dependencies": {
    "cardano-tessera-respond": "workspace:*"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "svelte": "^5.0.0",
    "svelte-check": "^4.1.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
