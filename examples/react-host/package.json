{
  "name": "example-react-host",
  "version": "0.0.0",
  "private": true,
  "description": "Minimal React host: reads a preprod survey bundle through cardano-tessera-client and renders it with <tessera-respond> via cardano-tessera-respond-react — built in CI so a compatibility regression fails a build instead of an integrator.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "type-check": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "cardano-tessera-client": "workspace:*",
    "cardano-tessera-respond-react": "workspace:*",
    "cip-179": "workspace:*",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
