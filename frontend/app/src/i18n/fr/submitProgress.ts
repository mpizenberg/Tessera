import type { Messages } from "../en/submitProgress";

const submitProgress: Messages = {
  srStep: "Étape {current} sur {total} : {label}",
  approveNote:
    "Approuvez la transaction dans votre portefeuille lorsque cela vous est demandé — ne fermez pas cet onglet.",
};

export default submitProgress;
