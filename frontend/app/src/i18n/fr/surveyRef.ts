import type { Messages } from "../en/surveyRef";

const surveyRef: Messages = {
  title:
    "Référence du sondage : la transaction qui le définit, et sa position dans celle-ci",
  label: "réf {ref}",
};

export default surveyRef;
