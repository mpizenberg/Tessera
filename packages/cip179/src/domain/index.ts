/**
 * CIP-179 domain — pure semantics over on-chain records.
 *
 * Dedupe, cancellation, credential-proof, audit and answer helpers defined over
 * the published on-chain record shapes ({@link ./records}). No I/O, no runtime
 * dependencies. Any CIP-179 implementation can reuse these to interpret the
 * same chain data the same way.
 *
 * @module
 */

export {};
