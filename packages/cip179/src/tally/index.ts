/**
 * CIP-179 tally — the reference ruleset and content-addressed artifact.
 *
 * The count and stake-weighted tally rules, the canonical-JSON encoding, and the
 * `RULESET_DESCRIPTOR` / `rulesetHash()` that make a tally result reproducible
 * and hash-identical across implementations.
 *
 * @module
 */

export {};
