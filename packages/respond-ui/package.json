{
  "name": "@tessera/respond-ui",
  "version": "0.1.0",
  "description": "Shared SolidJS answering UI for CIP-179 surveys: the per-question body components and their helpers, consumed by both the Tessera app (light DOM + CSS modules) and the <tessera-respond> widget (shadow DOM + literal class names). Styling and i18n are injected via context.",
  "type": "module",
  "private": true,
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "type-check": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@tessera/respond-core": "workspace:*",
    "cip-179": "workspace:*",
    "solid-js": "^1.9.14"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
