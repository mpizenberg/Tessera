{
  "name": "@tessera/tlock",
  "version": "0.1.0",
  "description": "Tessera sealed-survey tlock stack: drand quicknet round/time math, the lazy timelock encrypt/decrypt client, CBOR metadatum (de)serialization, and seal/reveal orchestration. Shared by the browser app, the serving tier, and the standalone verifier.",
  "type": "module",
  "private": true,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "type-check": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@tessera/core": "workspace:*",
    "cip-179": "workspace:*",
    "@evolution-sdk/evolution": "^0.5.9",
    "@mattpiz/tlock-js": "0.10.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
