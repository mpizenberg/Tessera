{
  "name": "cardano-tessera-mithril",
  "version": "0.0.0",
  "description": "Tessera's tally inputs read from a Mithril ancillary ledger state instead of Koios.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.15"
  },
  "scripts": {
    "facts": "tsx src/facts.ts",
    "compare": "tsx src/compare.ts",
    "verify": "tsx src/verify.ts",
    "type-check": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "cardano-tessera-client": "workspace:*",
    "cardano-tessera-core": "workspace:*",
    "cardano-tessera-koios": "workspace:*",
    "cborg": "^6.1.2",
    "cip-179": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.9.8",
    "tsx": "^4.23.15",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
