{
  "name": "@tessera/core",
  "version": "0.1.0",
  "description": "Tessera application core: the DataSource read seam, the Explore list/health payloads, keyset paging, and portable config. The reusable domain + tally surface lives in cip-179 and is imported directly, never re-exported here. No I/O, no runtime coupling.",
  "type": "module",
  "private": true,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "type-check": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "cip-179": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
